package com.example.fieldmaintenance.data.model

/**
 * Photos attached to a report (not tied to a specific asset).
 * Used by the "Monitoria y QR" module.
 */
enum class ReportPhotoType {
    QR_RESULT,
    NODE_PARAMS,
    OTHER_PARAMS
}


